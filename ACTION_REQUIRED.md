# Human actions required

## Independent annotation

Annotate the six cases in `fixtures/semantic-development-v1` with
`contract-eval annotate ...` without viewing Jev predictions first. Use two
independent annotators when practical and preserve both artifacts.

## Publication permission

Resolve TypeSafe/provider permission before publishing Jev scores, costs,
latency, raw output, or comparative benchmark claims.

## Future experiment approval

After annotation and importer validation, approve a fresh V2 evaluator
development protocol, then create a fresh heldout V2 cohort before considering
3–5 controlled Codex sessions.
